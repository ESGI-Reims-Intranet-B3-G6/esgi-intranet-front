import { Container, Typography } from '@mui/material';

const Hacklab = () => {
	return (
		<Container>
			<Typography>Page du Hacklab</Typography>
			<Typography>En construction!</Typography>
		</Container>
	);
};

export const Component = Hacklab;
