import { Container, Typography } from '@mui/material';

const News = () => {
	return (
		<Container>
			<Typography>Page des actualités</Typography>
			<Typography>En construction!</Typography>
		</Container>
	);
};

export const Component = News;
